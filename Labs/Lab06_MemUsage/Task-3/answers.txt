- if we use the default values, restrict doesnt change anything at all
- however if we go to N1=100, N2=8000000, then we see that we have 0.087 vs 0.078s so we get a performance gain
- if we go to N1=10, we loose that effect again